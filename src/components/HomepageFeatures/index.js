// This component is no longer used — the landing page uses GuideCards instead.
// Kept as an empty export to avoid breaking any lingering imports.
export default function HomepageFeatures() {
  return null;
}
